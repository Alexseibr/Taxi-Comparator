# AI-куратор калибровщика
Обновлено: 2026-05-02T08:05:18.241Z  · модель: gemini-2.5-flash

**Сводка.** За последний час зафиксировано 80 калибровок, из которых 23 (28.75%) помечены как подозрительные. Подавляющее большинство аномалий (19 из 23) относятся к категории 'vision_doubt', что указывает на системные проблемы с распознаванием данных.

## Метрики
- Калибровок просмотрено: **80**, подозрительных: **23** (28.7%)
- По категориям: vision_doubt=19, context_mismatch=1, price_outlier=2, demand_mismatch=1
- LOO median MAPE: —

## Хорошо
- Активный мониторинг: зафиксировано 80 калибровок за час.
- Низкое количество аномали��, связанных с контекстом, ценами и спросом (помимо 'vision_doubt').

## Тревожит
- Высокий процент подозрительных калибровок: 23 из 80 (28.75%).
- Доминирование аномалий 'vision_doubt' (19 случаев), указывающих на ошибки распознавания (etaMin > tripMin, factE > factC, нелогичные значения).
- Отсутствие актуальных данных по `looMedianMape` не позволяет оценить общую точность модели.

## Что сделать
- Срочно проверить и устранить причины аномалий 'vision_doubt', особенно те, где `etaMin` > `tripMin` или `factE` > `factC`.
- Проанализировать логи и скриншоты для последних 'high' и 'medium' `vision_doubt` случаев, чтобы выявить паттерны ошибок распознавания.
- Запустить пересчет `looMedianMape` для п��лучения актуальной оценки производительности модели.
- Рассмотреть возможность улучшения алгоритмов распознавания данных или качества исходных данных.
